#include <iostream>

using namespace std;

int main()

{int number;
     cout<<"enter your number";
     cin>>number;
      if(number>0)
        cout<<"posetive";
      else if(number< 0)
        cout<<"negetive";
      else if(number==0)
        cout<<"zero";

    return 0;
}
